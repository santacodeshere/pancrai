# PancrAI - Intelligent Pancreatic Tumor Detection System
# Requirements updated for Python 3.10-3.12 / 2025

# Core Framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.7.1

# Deep Learning (install whichever torch is available for your platform)
# CPU-only (smaller, always works):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# CUDA 12.x (if you have an NVIDIA GPU):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# Or just let pip pick the latest:
torch>=2.6.0
torchvision>=0.21.0
timm>=1.0.0
einops>=0.8.0

# Image Processing
opencv-python>=4.9.0
Pillow>=10.3.0
scikit-image>=0.22.0

# Scientific Computing
numpy>=1.26.0
scipy>=1.13.0
matplotlib>=3.8.0
scikit-learn>=1.4.0
pandas>=2.2.0

# Database
sqlalchemy>=2.0.30
aiosqlite>=0.20.0

# AI APIs (both have generous free tiers)
google-generativeai>=0.7.0
groq>=0.9.0

# Frontend
streamlit>=1.35.0
plotly>=5.22.0

# Utilities
python-dotenv>=1.0.1
httpx>=0.27.0
aiofiles>=23.2.1
requests>=2.32.0
tqdm>=4.66.0
reportlab>=4.2.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0

# ── Optional medical imaging libs (install separately if needed) ────────────
# These can be tricky to install on Windows — skip if not using DICOM/NIfTI
#
#   pip install pydicom          # DICOM file support (.dcm)
#   pip install nibabel          # NIfTI file support (.nii, .nii.gz)
#   pip install SimpleITK        # Advanced medical image I/O
#   pip install albumentations   # Faster augmentation pipeline
